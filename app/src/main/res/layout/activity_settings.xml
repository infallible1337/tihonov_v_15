<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <!-- Заголовок -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Настройки"
        android:textSize="24sp"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <!-- Пункт 1: Чекбокс по центру -->
    <CheckBox
        android:id="@+id/chkPolicyValidity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Показывать срок действия полиса"
        android:textSize="16sp"
        android:checked="true"
        android:buttonTint="@color/red"
        android:layout_marginBottom="16dp"
        android:layout_gravity="center" />

    <!-- Пункт 2: Чекбокс по центру -->
    <CheckBox
        android:id="@+id/chkActivePromotions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Показывать активные акции"
        android:textSize="16sp"
        android:checked="true"
        android:buttonTint="@color/red"
        android:layout_marginBottom="16dp"
        android:layout_gravity="center" />

    <!-- Пункт 3: Чекбокс по центру -->
    <CheckBox
        android:id="@+id/chkNotifyResults"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Сообщать о готовности анализов"
        android:textSize="16sp"
        android:checked="false"
        android:buttonTint="@color/red"
        android:layout_marginBottom="32dp"
        android:layout_gravity="center" />

    <!-- Разделитель -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/gray"
        android:layout_marginBottom="24dp" />

    <!-- Кнопка "Сбросить настройки" -->
    <Button
        android:id="@+id/btnResetSettings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Сбросить настройки"
        android:textColor="@color/white"
        android:backgroundTint="@color/red"
        android:padding="14dp"
        android:textSize="16sp"
        android:layout_marginBottom="24dp" />

    <!-- Кнопка "Назад" -->
    <Button
        android:id="@+id/btnBackMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Назад"
        android:textColor="@color/white"
        android:backgroundTint="@color/red"
        android:padding="14dp"
        android:textSize="16sp"
        android:layout_marginTop="0dp" />

</LinearLayout>